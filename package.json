{
  "name": "RESE-nextjs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "yarn --cwd ./packages/server dev",
    "client": "yarn --cwd ./packages/client dev"
  }
}
