{
  "name": "RESE-nextjs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "yarn --cwd ./packages/server dev",
    "next": "yarn --cwd ./packages/next-app dev"
  },
  "devDependencies": {
    "prettier": "2.7.1"
  }
}
