mutation LoginByToken {
  loginByToken {
    __typename
    ... on User {
      id
      fullName
      phone
      status
      token
      email
    }
    ... on loginByTokenError {
      params {
        ok
        status
      }
    }
  }
}
