mutation LoginByToken($token: String!) {
  loginByToken(token: $token) {
    __typename
    ... on User {
      id
      fullName
      phone
      userStatus: status
      token
      email
    }
    ... on Error {
      ok
      a: status
    }
  }
}
