type Query {
  greetings: String
}
type User {
  fullName: String
  id: Int
}

type loginError {
  error: Boolean
  email: String
  password: String
}

union A = User | loginError

type Mutation {
  login(email: String, password: String): A
}
